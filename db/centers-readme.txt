the centers.csv file stores the x,y offset for each image

it's added to when you run the vg center command

then you need to sort it ascending, and commit it to the repo

the columns are fileId,volume,x,y,radius
